FROM node:22-alpine AS base
WORKDIR /app

COPY server/package.json shared/package.json ./
RUN npm install

COPY shared/ ./shared/
COPY server/ ./server/
RUN npx prisma generate --schema=server/prisma/schema.prisma

EXPOSE 3001
CMD ["npx", "tsx", "server/src/index.ts"]

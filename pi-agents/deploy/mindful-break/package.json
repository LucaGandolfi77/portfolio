{
  "name": "mindful-break",
  "version": "1.0.0",
  "description": "A lightweight PWA for hourly breathing breaks. Zero data, zero backend, zero account.",
  "scripts": {
    "start": "npx serve . -l 8080",
    "lint": "node --check app.js"
  }
}
